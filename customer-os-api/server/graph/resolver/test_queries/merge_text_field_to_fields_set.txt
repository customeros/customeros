mutation MergeTextCustomFieldToFieldsSet($contactId: ID!, $fieldsSetId: ID!){
     mergeTextCustomFieldToFieldsSet(
        contactId: $contactId,
        fieldsSetId: $fieldsSetId,
        input: {name: "some name",
                value: "some value"}
     ) {
        id
        name
        value
      }
}