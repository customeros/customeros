name: Sync Slack Build (PR)

on:
  pull_request:
    branches:
      - otter
    paths:
      - "packages/runner/sync-slack/**"

env:
  APP_PATH: packages/runner/sync-slack
  CGO_ENABLED: 0
  LANG: C.UTF-8
  LC_ALL: C.UTF-8

permissions:
  pull-requests: read

jobs:
  build:
    runs-on: ubicloud-standard-2

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: ubicloud/setup-go@v5
        with:
          go-version: 1.24.2
          cache: true
        env:
          GOPROXY: "direct"
          GOSUMDB: "off"

      - name: Build Go Binary
        run: |
          ./deployment/runners/build.sh -p -g ${{ env.APP_PATH }}
