// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/openline-ai/openline-customer-os/packages/server/message-store/ent/messagefeed"
	"github.com/openline-ai/openline-customer-os/packages/server/message-store/ent/messageitem"
)

// MessageFeedCreate is the builder for creating a MessageFeed entity.
type MessageFeedCreate struct {
	config
	mutation *MessageFeedMutation
	hooks    []Hook
}

// SetUsername sets the "username" field.
func (mfc *MessageFeedCreate) SetUsername(s string) *MessageFeedCreate {
	mfc.mutation.SetUsername(s)
	return mfc
}

// AddMessageItemIDs adds the "message_item" edge to the MessageItem entity by IDs.
func (mfc *MessageFeedCreate) AddMessageItemIDs(ids ...int) *MessageFeedCreate {
	mfc.mutation.AddMessageItemIDs(ids...)
	return mfc
}

// AddMessageItem adds the "message_item" edges to the MessageItem entity.
func (mfc *MessageFeedCreate) AddMessageItem(m ...*MessageItem) *MessageFeedCreate {
	ids := make([]int, len(m))
	for i := range m {
		ids[i] = m[i].ID
	}
	return mfc.AddMessageItemIDs(ids...)
}

// Mutation returns the MessageFeedMutation object of the builder.
func (mfc *MessageFeedCreate) Mutation() *MessageFeedMutation {
	return mfc.mutation
}

// Save creates the MessageFeed in the database.
func (mfc *MessageFeedCreate) Save(ctx context.Context) (*MessageFeed, error) {
	var (
		err  error
		node *MessageFeed
	)
	if len(mfc.hooks) == 0 {
		if err = mfc.check(); err != nil {
			return nil, err
		}
		node, err = mfc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*MessageFeedMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = mfc.check(); err != nil {
				return nil, err
			}
			mfc.mutation = mutation
			if node, err = mfc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(mfc.hooks) - 1; i >= 0; i-- {
			if mfc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = mfc.hooks[i](mut)
		}
		v, err := mut.Mutate(ctx, mfc.mutation)
		if err != nil {
			return nil, err
		}
		nv, ok := v.(*MessageFeed)
		if !ok {
			return nil, fmt.Errorf("unexpected node type %T returned from MessageFeedMutation", v)
		}
		node = nv
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (mfc *MessageFeedCreate) SaveX(ctx context.Context) *MessageFeed {
	v, err := mfc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (mfc *MessageFeedCreate) Exec(ctx context.Context) error {
	_, err := mfc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (mfc *MessageFeedCreate) ExecX(ctx context.Context) {
	if err := mfc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (mfc *MessageFeedCreate) check() error {
	if _, ok := mfc.mutation.Username(); !ok {
		return &ValidationError{Name: "username", err: errors.New(`ent: missing required field "MessageFeed.username"`)}
	}
	return nil
}

func (mfc *MessageFeedCreate) sqlSave(ctx context.Context) (*MessageFeed, error) {
	_node, _spec := mfc.createSpec()
	if err := sqlgraph.CreateNode(ctx, mfc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (mfc *MessageFeedCreate) createSpec() (*MessageFeed, *sqlgraph.CreateSpec) {
	var (
		_node = &MessageFeed{config: mfc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: messagefeed.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: messagefeed.FieldID,
			},
		}
	)
	if value, ok := mfc.mutation.Username(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: messagefeed.FieldUsername,
		})
		_node.Username = value
	}
	if nodes := mfc.mutation.MessageItemIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2M,
			Inverse: false,
			Table:   messagefeed.MessageItemTable,
			Columns: []string{messagefeed.MessageItemColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeInt,
					Column: messageitem.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// MessageFeedCreateBulk is the builder for creating many MessageFeed entities in bulk.
type MessageFeedCreateBulk struct {
	config
	builders []*MessageFeedCreate
}

// Save creates the MessageFeed entities in the database.
func (mfcb *MessageFeedCreateBulk) Save(ctx context.Context) ([]*MessageFeed, error) {
	specs := make([]*sqlgraph.CreateSpec, len(mfcb.builders))
	nodes := make([]*MessageFeed, len(mfcb.builders))
	mutators := make([]Mutator, len(mfcb.builders))
	for i := range mfcb.builders {
		func(i int, root context.Context) {
			builder := mfcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*MessageFeedMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, mfcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, mfcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, mfcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (mfcb *MessageFeedCreateBulk) SaveX(ctx context.Context) []*MessageFeed {
	v, err := mfcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (mfcb *MessageFeedCreateBulk) Exec(ctx context.Context) error {
	_, err := mfcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (mfcb *MessageFeedCreateBulk) ExecX(ctx context.Context) {
	if err := mfcb.Exec(ctx); err != nil {
		panic(err)
	}
}
