mutation {
  tag_Create(input: {
                        name: "the tag"
                    }) {
      metadata {
        id
        created
        lastUpdated
        source
        appSource
      }
      name
    }
}