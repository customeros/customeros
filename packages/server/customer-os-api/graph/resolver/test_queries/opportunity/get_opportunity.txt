query GetOpportunity($opportunityId: ID!){
  opportunity(id: $opportunityId) {
    metadata {
        id
        created
        lastUpdated
    }
    name
    amount
    maxAmount
    renewalApproved
    renewalAdjustedRate
    createdBy {
        id
    }
    owner {
        id
    }
  }
}