query GetContactById($contactId: ID!){
  contact(id: $contactId) {
    id
    tickets {
          id
          subject
          status
          priority
          description
          tags {
            id
            name
          }
    }
  }
}