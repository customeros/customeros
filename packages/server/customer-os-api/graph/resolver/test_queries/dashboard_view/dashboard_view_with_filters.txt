query GetDashboardData($page: Int!, $limit: Int!, $searchTerm: String!) {
  dashboardView(pagination: {page: $page, limit: $limit}, searchTerm: $searchTerm) {
    content {
      contact {
        id
        firstName
        lastName
        emails {
          id
          email
        }
      }
      organization {
        id
        name
      }
    }
    totalPages
    totalElements
  }
}