mutation SetHealthIndicator($organizationId: ID!, $healthIndicatorId: ID!) {
  organization_SetHealthIndicator(organizationId: $organizationId, healthIndicatorId: $healthIndicatorId) {
    id
    updatedAt
    healthIndicator {
        id
    }
  }
}