mutation UpdateEmailForUser($userId: ID!, $emailId: ID!) {
    emailUpdateInUser(userId: $userId,
                        input: {id: $emailId
                                label: HOME
                                primary: true
                        }) {
        id
        email
        rawEmail
        label
        primary
        updatedAt
    }
}