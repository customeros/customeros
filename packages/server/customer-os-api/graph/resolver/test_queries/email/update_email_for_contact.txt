mutation UpdateEmailForContact($contactId: ID!, $emailId: ID!) {
    emailUpdateInContact(contactId: $contactId,
                        input: {id: $emailId
                                email: "new@email.com"
                                label: HOME
                                primary: true
                        }) {
        id
        email
        label
        primary
        updatedAt
    }
}