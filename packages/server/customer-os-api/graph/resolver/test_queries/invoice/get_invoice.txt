query GetInvoice($id: ID!) {
invoice(id: $id) {
    id
    createdAt
    updatedAt
    dryRun
    number
    periodStartDate
    periodEndDate
    dueDate
    amount
    vat
    total
    currency
    repositoryFileId

    invoiceLines {
        name
        price
        quantity
        amount
        vat
        total
    }
  }
}