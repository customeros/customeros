query GetInvoice($id: ID!) {
invoice(id: $id) {
    id
    createdAt
    updatedAt
    dryRun
    number
    periodStartDate
    periodEndDate
    dueDate
    amount
    vat
    totalAmount
    currency
    repositoryFileId

    invoiceLines {
        name
        price
        quantity
        amount
        vat
        totalAmount
    }
  }
}