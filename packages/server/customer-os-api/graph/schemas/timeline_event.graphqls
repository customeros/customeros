union TimelineEvent = PageView | InteractionSession | Ticket | Conversation | Note | InteractionEvent | Analysis | Issue

enum TimelineEventType {
    PAGE_VIEW
    INTERACTION_SESSION
    TICKET @deprecated(reason: "Use Issue instead")
    CONVERSATION
    NOTE
    INTERACTION_EVENT
    ANALYSIS
    ISSUE
}