mutation saveAgent($input: AgentSaveInput!) {
  agent_Save(input: $input) {
    id
    type
    name
    capabilities {
      id
      type
      name
      action
      errors
    }
    goal
    isActive
    flowId
    visible
    createdAt
    updatedAt
    error
    color
    icon
  }
}