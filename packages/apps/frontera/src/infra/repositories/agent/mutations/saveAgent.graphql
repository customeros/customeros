mutation saveAgent($input: AgentSaveInput!) {
  agent_Save(input: $input) {
    id
    type
    name
    listeners {
      id
      type
      name
      active
      config
      errors
    }
    capabilities {
      id
      type
      name
      active
      config
      errors
    }
    goal
    goalType
    isActive
    flowId
    visible
    createdAt
    updatedAt
    error
    color
    scope
    icon
    isConfigured
  }
}
