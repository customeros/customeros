query agent($id: ID!) {
  agent(id: $id) {
    id
    type
    name
    capabilities {
      id
      type
      name
      action
      errors
    }
    goal
    isActive
    flowId
    visible
    createdAt
    updatedAt
    error
    color
    icon
  }
}