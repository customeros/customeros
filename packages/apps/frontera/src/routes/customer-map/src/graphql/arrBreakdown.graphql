query ArrBreakdown($period: DashboardPeriodInput) {
  dashboard_ARRBreakdown(period: $period) {
    arrBreakdown
    increasePercentage
    perMonth {
      year
      month
      newlyContracted
      renewals
      upsells
      downgrades
      cancellations
      churned
    }
  }
}
