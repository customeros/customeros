query masterPlans {
  masterPlans {
    id
    name
    retired
    milestones {
      id
      name
      order
      durationHours
      optional
      items
      retired
    }
    retiredMilestones {
      id
      name
      order
      durationHours
      optional
      items
      retired
    }
  }
}
