mutation createBankAccount($input: BankAccountCreateInput!) {
  bankAccount_Create(input: $input) {
    currency
    bankName
    bankTransferEnabled
    iban
    bic
    sortCode
    accountNumber
    routingNumber
  }
}
