mutation updateBankAccount($input: BankAccountUpdateInput!) {
  bankAccount_Update(input: $input) {
    currency
    bankName
    bankTransferEnabled
    iban
    bic
    sortCode
    accountNumber
    routingNumber
  }
}
