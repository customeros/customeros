mutation createFlow($input: FlowSequenceStoreInput!) {
  flow_sequence_Store(input: $input) {
    name
    description
    flow {
      metadata {
        id
      }
      name
      description
      status
    }
    status
    metadata {
      id
    }
  }
}
