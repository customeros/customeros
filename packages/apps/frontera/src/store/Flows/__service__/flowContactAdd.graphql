mutation flowContactAdd($flowId: ID!, $contactId: ID!) {
  flowContact_Add(flowId: $flowId, contactId: $contactId) {
    status
    scheduledAction
    scheduledAt
    metadata {
      id
    }
    contact {
      metadata {
          id
      }
    }
  }
}
