mutation FlowSequenceLinkContact($sequenceId: ID!, $contactId: ID!,$emailId: ID!) {
  flow_sequence_LinkContact(sequenceId: $sequenceId, contactId: $contactId,emailId: $emailId) {
    metadata {
      id
    }
    contact {
      metadata {
        id
      }
    }
    email {
       id
    }
  }
}