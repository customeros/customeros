query LoadTimelineForOrganization($id: ID!) {
  organization(id: $id) {
    contacts {
      content {
        notes {
          content {
            id
            html
          }
        }
        conversations(
          pagination: { page: 0, limit: 25 }
          sort: { by: "STARTED_AT", direction: DESC }
        ) {
          content {
            id
            subject
          }
        }
        tickets {
          id
          createdAt
          updatedAt
          subject
          status
          priority
          description
          tags {
            id
            name
          }
          notes {
            id
            createdAt
            html
          }
        }
      }
    }
  }
}
