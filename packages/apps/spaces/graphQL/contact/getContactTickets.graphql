query GetContactTickets($id: ID!) {
  contact(id: $id) {
    tickets {
      id
      createdAt
      updatedAt
      subject
      status
      priority
      description
      tags {
        id
        name
      }
      notes {
        id
        createdAt
        html
      }
    }
  }
}
