query GetContact($id: ID!) {
    contact(id: $id) {
        ...ContactPersonalDetails
        owner {
            id
            firstName
            lastName
        }
        ...ContactCommunicationChannelsDetails
        jobRoles {
            ...JobRole
        }
        source
        locations {
            ...LocationBaseDetails
            rawAddress
        }
    }
}
