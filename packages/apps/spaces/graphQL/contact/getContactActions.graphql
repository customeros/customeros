query GetActionsForContact($id: ID!, $from: Time!, $to: Time!) {
  contact(id: $id) {
    id
    firstName
    lastName
    createdAt
    actions(from: $from, to: $to) {
      ... on PageView {
        id
        application
        startedAt
        endedAt
        engagedTime
        pageUrl
        pageTitle
        orderInSession
        sessionId
      }
    }
  }
}
