query GetEmailValidation($id: ID!){
    email(id: $id) {
        id
        emailValidationDetails {
            isReachable
            isValidSyntax
            canConnectSmtp
            acceptsMail
            hasFullInbox
            isCatchAll
            isDeliverable
            validated
            isDisabled
        }
    }
}
