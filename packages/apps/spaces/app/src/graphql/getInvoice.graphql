query GetInvoice($id: ID!) {
  invoice(id: $id) {
    id
    createdAt
    status
    number
    periodStartDate
    periodEndDate
    dueDate
    amount
    vat
    total
    currency
    repositoryFileId
    invoiceLines {
      id
      createdAt
      quantity
      amount
      vat
      total
      price
      name
    }
  }
}
