mutation createReminder($input: ReminderInput!) {
  reminder_Create(input: $input) {
    metadata {
      id
    }
    content
    owner {
      id
      firstName
      lastName
      name
    }
    dueDate
    dismissed
  }
}
