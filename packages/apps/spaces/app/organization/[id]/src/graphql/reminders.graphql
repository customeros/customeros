query reminders($organizationId: ID!) {
  remindersForOrganization(organizationId: $organizationId) {
    metadata {
      id
    }
    content
    owner {
      id
      firstName
      lastName
      name
    }
    dueDate
    dismissed
  }
}
