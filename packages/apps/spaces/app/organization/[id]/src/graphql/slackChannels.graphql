query slackChannels($pagination: Pagination) {
  slack_Channels(pagination: $pagination) {
    content {
      channelId
      organization {
        metadata {
          id
        }
      }
    }
    totalElements
  }
}
