{
  "groups": [
    {
      "name": "MeetingScheduling",
      "queries": [
        {
          "name": "Meeting scheduling records with non-existent tenants",
          "query": "SELECT COUNT(*) FROM meeting_scheduling ms WHERE NOT EXISTS (SELECT 1 FROM tenant t WHERE t.id = ms.tenant_id)",
        }
      ]
    }
  ]
} 